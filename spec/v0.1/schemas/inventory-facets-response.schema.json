{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://autoagentprotocol.org/v0.1/schemas/inventory-facets-response.schema.json",
  "title": "InventoryFacetsResponse",
  "description": "Typed AAP response for the `inventory.facets` skill. The `data` object is a Facets aggregation. Carried inside an A2A `Message.parts[].data` DataPart returned from the `SendMessage` operation.",
  "type": "object",
  "properties": {
    "type": { "const": "inventory.facets.response" },
    "data": { "$ref": "./facets.schema.json" },
    "message": {
      "type": "string",
      "description": "Optional contextual note. MAY be omitted.",
      "maxLength": 2000
    }
  },
  "required": ["type", "data"],
  "additionalProperties": false
}
