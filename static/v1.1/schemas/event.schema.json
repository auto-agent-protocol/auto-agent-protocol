{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://autoagentprotocol.org/v1.1/schemas/event.schema.json",
  "title": "Event",
  "description": "Minimal AAP event envelope. Used for asynchronous status updates (e.g. lead status changes, appointment confirmations) delivered via A2A push notifications or task status update events.",
  "type": "object",
  "properties": {
    "type": { "const": "aap.event" },
    "event_kind": {
      "type": "string",
      "enum": ["lead.status_changed", "appointment.status_changed"],
      "description": "Concrete event kind. v1.0 defines two kinds; future versions may add more.",
      "examples": ["lead.status_changed", "appointment.status_changed"]
    },
    "entity_id": {
      "type": "string",
      "description": "Identifier of the affected entity (lead_id or appointment_id).",
      "examples": ["lead_8f3c1a", "appt_2b7d9e"]
    },
    "status": {
      "type": "string",
      "description": "New status value for the entity. For 'lead.status_changed', one of: received | duplicate | rejected | working | sold_to | lost. For 'appointment.status_changed', one of: requested | proposed | confirmed | rejected | completed | no_show. Conditional `if/then` clauses below enforce the right subset for each `event_kind`.",
      "examples": ["received", "confirmed"]
    },
    "occurred_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 / RFC 3339 timestamp at which the event occurred (e.g. '2026-04-30T10:15:30Z'). MUST include a timezone offset (Z or ±HH:MM).",
      "examples": ["2026-04-30T10:15:30Z"]
    },
    "payload": {
      "type": "object",
      "description": "Optional event-specific payload. Schema is event-kind-dependent and dealer-defined.",
      "additionalProperties": true
    }
  },
  "required": ["type", "event_kind", "entity_id", "status", "occurred_at"],
  "additionalProperties": false,
  "allOf": [
    {
      "description": "lead.status_changed restricts status to the lead-status vocabulary",
      "if": {
        "type": "object",
        "properties": { "event_kind": { "const": "lead.status_changed" } },
        "required": ["event_kind"]
      },
      "then": {
        "properties": {
          "status": {
            "enum": ["received", "duplicate", "rejected", "working", "sold_to", "lost"]
          }
        }
      }
    },
    {
      "description": "appointment.status_changed restricts status to the appointment-status vocabulary",
      "if": {
        "type": "object",
        "properties": { "event_kind": { "const": "appointment.status_changed" } },
        "required": ["event_kind"]
      },
      "then": {
        "properties": {
          "status": {
            "enum": ["requested", "proposed", "confirmed", "rejected", "completed", "no_show"]
          }
        }
      }
    }
  ]
}
